with "GWindows";

library project Vulkan is
   for Library_Name use "vulkan_ada";
   for Library_Kind use "static";

   for Source_Dirs  use ("src/win32");
   for Object_Dir   use "obj";
   for Library_Dir  use "lib";

   for Source_Files use (
                         "vk.ads",
			 "generic_address_to_access_conversions.adb",
			 "generic_address_to_access_conversions.ads"
			);

   for Languages    use ("Ada");

end Vulkan;
